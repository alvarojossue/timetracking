<h1>My projects</h1>
<ul>
	<% @projects.each do |project| %>
		<% if project.description != nil %>
			<li><%= project.name + " - " + project.description%></li>
		<% else %>
			<li><%= project.name %>
		<% end %>
	<% end %>
</ul>